import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    borderWidth: 1,
    borderRadius: 2,
    borderColor: '#BDBDBD',
    borderBottomWidth: 0,
    marginHorizontal: 10,
    marginVertical: 20,
  },
});

export default styles;
